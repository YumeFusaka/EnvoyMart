<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>yumefusaka.envoymart</groupId>
    <artifactId>envoymart-services</artifactId>
    <version>0.1.0-SNAPSHOT</version>
    <packaging>pom</packaging>
    <name>envoymart-services</name>

    <modules>
        <module>common</module>
        <module>auth-service</module>
        <module>product-service</module>
        <module>order-service</module>
        <module>payment-service</module>
        <module>review-service</module>
        <module>agent-core</module>
        <module>ai-service</module>
        <module>gateway-service</module>
    </modules>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
        <java.version>21</java.version>
        <spring.boot.version>4.1.1</spring.boot.version>
        <spring.cloud.version>2025.1.3</spring.cloud.version>
        <spring.cloud.alibaba.version>2025.1.0.0</spring.cloud.alibaba.version>
        <!-- LangChain4j：AI 接入层。核心是正式版，Milvus/MCP 等集成模块仍在 beta 线 -->
        <langchain4j.version>1.20.0</langchain4j.version>
        <mcp.sdk.version>2.0.1</mcp.sdk.version>
        <mybatis.plus.version>3.5.17</mybatis.plus.version>
        <redisson.version>4.7.0</redisson.version>
        <jjwt.version>0.13.0</jjwt.version>
        <langgraph4j.version>1.8.27</langgraph4j.version>
        <springdoc.version>3.1.1</springdoc.version>
        <lombok.version>1.18.42</lombok.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-dependencies</artifactId>
                <version>${spring.boot.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <dependency>
                <groupId>org.springframework.cloud</groupId>
                <artifactId>spring-cloud-dependencies</artifactId>
                <version>${spring.cloud.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <dependency>
                <groupId>com.alibaba.cloud</groupId>
                <artifactId>spring-cloud-alibaba-dependencies</artifactId>
                <version>${spring.cloud.alibaba.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <!--
                必须在 langgraph4j 之前生效：langgraph4j 1.8.27 的 parent pom 把
                langchain4j 版本 pin 在 1.19.0，会作为传递依赖带进来。
                这里 import 的 BOM 覆盖传递依赖版本，把它拉到 1.20.0。
            -->
            <dependency>
                <groupId>dev.langchain4j</groupId>
                <artifactId>langchain4j-bom</artifactId>
                <version>${langchain4j.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.springframework.boot</groupId>
                    <artifactId>spring-boot-maven-plugin</artifactId>
                    <version>${spring.boot.version}</version>
                    <!-- repackage 会把普通 jar 重新打成「可执行 jar」（补上 Main-Class 与依赖）。
                         本项目的父 pom 不是 spring-boot-starter-parent（模块是手写的），
                         所以这个 goal 不会被自动绑定，必须显式声明 —— 否则打出来的 jar
                         只有类文件、没有入口，容器里一跑就报 "no main manifest attribute"。
                         放在 pluginManagement 里：7 个服务模块各自声明了本插件，会自动继承；
                         common / agent-core 是库、没声明，因此不受影响（它们没有 main class）。 -->
                    <executions>
                        <execution>
                            <goals>
                                <goal>repackage</goal>
                            </goals>
                        </execution>
                    </executions>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-compiler-plugin</artifactId>
                    <version>3.14.1</version>
                    <configuration>
                        <release>${java.version}</release>
                        <!-- 保留方法参数名，@RequestParam / @PathVariable 才能省略 name -->
                        <parameters>true</parameters>
                        <annotationProcessorPaths>
                            <path>
                                <groupId>org.projectlombok</groupId>
                                <artifactId>lombok</artifactId>
                                <version>${lombok.version}</version>
                            </path>
                        </annotationProcessorPaths>
                    </configuration>
                </plugin>
            </plugins>
        </pluginManagement>

        <!--
            编译器插件必须在这里再声明一次才会生效。
            pluginManagement 只提供「默认配置」，作用于**确实被声明的插件**；
            上面那份配置不会自动激活任何东西——它只是让子模块声明时可以省略版本与配置。
            此前配置只写在 pluginManagement 里且没有任何模块声明该插件，
            结果 -parameters 从未真正传入编译器：省略 name 的 @RequestParam / @PathVariable
            在运行时抛 "Name for argument ... not specified"，而且要到实际调用那个接口才暴露。
        -->
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
            </plugin>
        </plugins>
    </build>
</project>
