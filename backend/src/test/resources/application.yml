spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:envoymart-test;MODE=MySQL;DATABASE_TO_LOWER=TRUE;CASE_INSENSITIVE_IDENTIFIERS=TRUE;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    password:
  sql:
    init:
      mode: always
      encoding: UTF-8

logging:
  level:
    root: warn

jwt:
  token-name: token
  ttl: 432000000000
  secret-key: yumefusaka
